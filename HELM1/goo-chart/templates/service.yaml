apiVersion: v1
kind: Service
metadata:
  name: {{ include "goo-chart.fullname" . }}
  labels:
    {{- include "goo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goo-chart.selectorLabels" . | nindent 4 }}
